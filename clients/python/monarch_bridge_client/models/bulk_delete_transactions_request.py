# coding: utf-8

"""
    Monarch Bridge

    HTTP/JSON wrapper over Monarch Money's GraphQL API.  **Contract conventions:** - Every successful 2xx body is `{\"data\": <typed object>}` (except `/healthz`   and `/v1/auth/{status,refresh}`, which return plain bodies so health probes   stay zero-parse). - Failure bodies, when returned, are `{\"code\": ..., \"message\": ..., \"details\": {...}}`. - Mutations whose application layer reports errors come back as `HTTP 422`   (the proxy walks Monarch's `data.<mutation>.errors[]` and surfaces it). - No per-request auth on the wire - the bridge holds Monarch's session token   itself, loaded once via the interactive bootstrap CLI.  **Source of truth.** This multi-file YAML tree is hand-curated and authoritative. Both request DTOs and response DTOs are generated from it via openapi-generator-gradle-plugin into `build/generated/openapi/`, along with a Spring `*Api` interface per tag that controllers implement directly. The spec drives the web layer end-to-end.  Full agent reference + every Monarch quirk: see `agents/context/AGENTS.md`. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from monarch_bridge_client.models.bulk_transaction_filters import BulkTransactionFilters
from typing import Optional, Set
from typing_extensions import Self

class BulkDeleteTransactionsRequest(BaseModel):
    """
    Body for `POST /v1/transactions/bulk-delete`. Mirrors Monarch's `bulkDeleteTransactions` mutation variables. Two selection modes:  - **Explicit** (`allSelected: false`, default): `selectedTransactionIds`   lists exactly which rows to delete. - **Filter-based** (`allSelected: true`): every row matching `filters` is   deleted, minus the optional `excludedTransactionIds` set.  `expectedAffectedTransactionCount` is required as a safety guard. Pass the number of rows you believe will be affected. Monarch rejects the mutation if the actual count differs (race-prevention). 
    """ # noqa: E501
    selected_transaction_ids: Optional[List[StrictStr]] = Field(default=None, description="Ids to delete (explicit mode). Ignored when `allSelected: true`.", alias="selectedTransactionIds")
    excluded_transaction_ids: Optional[List[StrictStr]] = Field(default=None, description="Ids to skip (filter mode). Ignored when `allSelected: false`.", alias="excludedTransactionIds")
    all_selected: Optional[StrictBool] = Field(default=False, description="Switch between explicit (false) and filter (true) selection.", alias="allSelected")
    expected_affected_transaction_count: StrictInt = Field(description="Safety check. Monarch refuses if actual affected count differs.", alias="expectedAffectedTransactionCount")
    filters: Optional[BulkTransactionFilters] = None
    __properties: ClassVar[List[str]] = ["selectedTransactionIds", "excludedTransactionIds", "allSelected", "expectedAffectedTransactionCount", "filters"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BulkDeleteTransactionsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of filters
        if self.filters:
            _dict['filters'] = self.filters.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BulkDeleteTransactionsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "selectedTransactionIds": obj.get("selectedTransactionIds"),
            "excludedTransactionIds": obj.get("excludedTransactionIds"),
            "allSelected": obj.get("allSelected") if obj.get("allSelected") is not None else False,
            "expectedAffectedTransactionCount": obj.get("expectedAffectedTransactionCount"),
            "filters": BulkTransactionFilters.from_dict(obj["filters"]) if obj.get("filters") is not None else None
        })
        return _obj


