mutation Web_TestCreditScoreChangedNotification($previousScore: Int!, $currentScore: Int!) {
    sendCreditScoreNotification(previousScore: $previousScore, currentScore: $currentScore) {
      success
    }
  }
