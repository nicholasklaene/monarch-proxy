query Web_GetScheduleCLineItems($taxYear: Int!) {
    scheduleCLineItems(taxYear: $taxYear) {
      key
      lineNumber
      description
      lineType
      sortOrder
      isNotTracked
      displayInfo
    }
  }
