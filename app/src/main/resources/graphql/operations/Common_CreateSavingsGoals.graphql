mutation Common_CreateSavingsGoals($input: CreateSavingsGoalsInput!) {
  createSavingsGoals(input: $input) {
    savingsGoals {
      id
      name
      type
      status
      targetAmount
      targetDate
      currentBalance
      __typename
    }
    __typename
  }
}
