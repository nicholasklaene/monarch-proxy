query Web_GetRecommendedCategories($transactionId: ID!) {
    recommendedCategories(id: $transactionId) {
      occurrences
      category {
        id
        name
        order
        icon
        group {
          id
          type
        }
      }
    }
  }
