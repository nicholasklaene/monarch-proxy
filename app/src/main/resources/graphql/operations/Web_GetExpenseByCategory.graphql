query Web_GetExpenseByCategory($startDate: String, $endDate: String) {
  expenseByCategory(startDate: $startDate, endDate: $endDate) {
    categoryId
    categoryName
    amount
    transactionCount
    __typename
  }
}
