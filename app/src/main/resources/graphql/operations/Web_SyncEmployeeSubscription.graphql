mutation Web_SyncEmployeeSubscription {
    syncEmployeeSubscription {
      subscription {
        id
        entitlements
      }
      errors {
        ...PayloadErrorFields
      }
    }
  }

fragment PayloadErrorFields on PayloadError {
    fieldErrors {
      field
      messages
    }
    message
    code
  }
